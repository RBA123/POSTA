// Re-export Firestore types from parent project
export type {
  User,
  Market,
  MarketCategory,
  MarketStatus,
  MarketResult,
  UserBet,
  BetStatus,
  Transaction,
  CreateMarketInput,
} from "../../firebase/types/firestore.types";
